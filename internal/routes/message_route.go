package routes

func (routes *Routes) MessageRoutes() {
	// handler := handlers.NewMessage(routes.container)
	// middle := middleware.New(routes.container)
	// group := routes.server.Group("message").Use(middle.AuthMiddleware())
	// {
	// 	group.GET("/:uuid", handler.Get)
	// }
}
